import express from "express";
import dummyController from "../controllers/dummy.controller.js";

const router = express();
// router.get("/dashboard-bk", dummyController.dashboardBk);
// router.get("/laporan", dummyController.laporan);
// router.get("/status", dummyController.status);
// router.get("/laporan-details", dummyController.detailsLaporan);
// router.post("/submit-reports", dummyController.submitFormLaporan);
export default router;
